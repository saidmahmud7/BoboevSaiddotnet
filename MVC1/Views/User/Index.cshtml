@model List<MVC1.Models.UserModel>

    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-primary fw-bold">Список пользователей</h2>
            <a href="/User/Create" class="btn btn-success btn-lg shadow">
                <i class="bi bi-plus-circle me-2"></i>Добавить пользователя
            </a>
        </div>

        @if (Model != null && Model.Count > 0)
        {
            <div class="table-responsive">
                <table class="table table-hover table-bordered align-middle shadow rounded">
                    <thead class="table-gradient text-white">
                        <tr>
                            <th class="text-center">🎯 ID</th>
                            <th>🌟 Имя</th>
                            <th>🌟 Фамилия</th>
                            <th>📧 Email</th>
                            <th class="text-center">🎂 Возраст</th>
                            <th class="text-center">⚙️ Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td class="text-center fw-bold bg-light">@user.Id</td>
                                <td class="bg-light">@user.FisrtName</td>
                                <td class="bg-light">@user.LatName</td>
                                <td class="bg-light">@user.Email</td>
                                <td class="text-center bg-light">@user.Age</td>
                                <td class="text-center bg-light">
                                    <div class="btn-group" role="group">
                                        <a href="/User/Details/@user.Id" class="btn btn-info btn-sm">
                                            <i class="bi bi-eye me-1"></i>Подробнее
                                        </a>
                                        <a href="/User/Update/@user.Id" class="btn btn-warning btn-sm">
                                            <i class="bi bi-pencil me-1"></i>Редактировать
                                        </a>
                                        <form action="/User/Delete" method="post" class="d-inline">
                                            <input type="hidden" name="id" value="@user.Id" />
                                            <button type="submit" class="btn btn-danger btn-sm">
                                                <i class="bi bi-trash me-1"></i>Удалить
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="alert alert-warning text-center shadow-sm" role="alert">
                <i class="bi bi-exclamation-triangle me-2 text-warning"></i><strong>Нет пользователей!</strong>
            </div>
        }
    </div>
<style>
    .table-gradient {
        background: linear-gradient(45deg, #6a11cb, #2575fc);
    }

    .table th,
    .table td {
        vertical-align: middle;
    }

    h2 {
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
    }
</style>

<!-- Add Bootstrap Icons CDN for icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
